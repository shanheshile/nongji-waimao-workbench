# 贡献指南

感谢参与农机外贸工作台。

## 开始之前

1. 先搜索现有 Issue，避免重复工作。
2. 功能提案请说明用户问题、数据边界、失败状态与验收方式。
3. 不要提交从企业系统导出的界面、响应、日志、素材或目录。
4. 新增演示内容必须明显标记“DEMO/虚构”，且不能影射真实客户、品牌或商品。

## 本地验证

```bash
pnpm install --frozen-lockfile
pnpm run ci
```

提交前请确认工作区没有 `dist/`、日志、编辑器配置、环境文件或其他生成物。

## 代码要求

- 保持中文界面可读，英文参数仅作为原版对照；
- 金额、税费、重量、尺寸和包装字段必须写明单位与口径；
- 公式输入改变后应立即更新结果；
- 税率为 0 时不得显示“已核验免税”；
- 匹配必须基于显式演示关系，不能默认选择搜索第一项；
- 禁止加入登录、密码记忆或默认生产连接；
- 所有图片必须是许可清楚的仓库内本地文件。

## Pull Request

请保持每个 PR 聚焦一个问题，描述已运行的检查，并使用仓库模板逐项确认。贡献一经接受，将按 Apache License 2.0 发布。
